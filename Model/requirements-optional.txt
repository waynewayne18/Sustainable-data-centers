# Optional — install per role, not all at once.
# Nothing here is needed to run skeleton.py.

# --- Intelligence: planning application extraction -------------------
anthropic                # LLM extraction from application text
beautifulsoup4           # only if PlanIt's API doesn't cover an authority
lxml                     # faster parser for the above

# --- Ingest: OpenStreetMap footprints --------------------------------
osmnx==2.1.1             # industrial parcels, building footprints

# --- Satellite: only if you keep the imagery work --------------------
pystac-client==0.9.0     # STAC catalogue search
planetary-computer==1.0.0  # signs Planetary Computer asset URLs
odc-stac==0.5.3          # loads STAC items into xarray
rioxarray==0.19.0        # raster/xarray bridge
xarray==2026.7.0

# --- SAR change detection: only if it survives triage ----------------
# torch                  # large download; skip unless committed
# torchvision

# --- Working comfortably ---------------------------------------------
jupyterlab               # genuinely worth it — inspecting layers
                         # interactively beats re-running a script
